import { type } from "os";

export type TMaterialColor = "primary" | "inherit" | "default" | "secondary" | undefined
export type TMaterialVariant = "contained" | "text" | "outlined" | undefined

export type favourite = {
  id: string;
  text: string;
}